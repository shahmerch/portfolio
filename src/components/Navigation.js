import React from 'react';
import { motion } from 'framer-motion';

const Navigation = () => {
  return (
    <nav className="hidden">
      {/* This component is currently not used but can be extended for additional navigation features */}
    </nav>
  );
};

export default Navigation; 